@AGENTS.md

## Propre à Claude Code

- Les permissions accordées sur cette machine sont dans `.claude/`, que le
  `.gitignore` de ce dépôt tient hors du suivi.
- `dist-public/` est servi publiquement sur `technohab.theoseguret.fr` par une
  allowlist ([D-032](https://github.com/HolaHombre/MetaProjet)) : ce qui y entre
  devient lisible par tous. Le jeu de plans, les profils, les scripts et `.git/`
  en sont exclus et doivent le rester.
